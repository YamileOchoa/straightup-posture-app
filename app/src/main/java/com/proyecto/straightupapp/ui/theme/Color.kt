package com.proyecto.straightupapp.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════════════════
// PALETA PRINCIPAL - Verde Salud (Material Design 3)
// Inspirada en apps de wellness: Headspace, Calm, Apple Health
// ═══════════════════════════════════════════════════════════════════

// Verde Principal - Tonos más suaves y profesionales
val GreenPrimary = Color(0xFF4CAF50)      // Verde balanceado (Material Green 500)
val GreenLight = Color(0xFFE8F5E9)        // Verde muy claro (fondo suave)
val GreenDark = Color(0xFF2E7D32)         // Verde profundo (contraste fuerte)
val GreenAccent = Color(0xFF66BB6A)       // Verde claro vibrante (CTAs secundarios)

// Verde Secundario - Para variaciones
val GreenContainer = Color(0xFFC8E6C9)    // Contenedores destacados
val GreenOnContainer = Color(0xFF1B5E20)  // Texto sobre contenedores verdes

// Neutros Premium - Más refinados
val White = Color(0xFFFFFFFF)
val OffWhite = Color(0xFFFAFAFA)          // Fondo principal (más cálido)
val Surface = Color(0xFFF5F5F5)           // Tarjetas y superficies
val SurfaceVariant = Color(0xFFEEEEEE)    // Variación de superficie

// Grises con mejor contraste
val GrayLight = Color(0xFFE0E0E0)
val GrayMedium = Color(0xFF757575)        // Mejor contraste para texto secundario
val GrayDark = Color(0xFF212121)          // Texto principal (casi negro)
val GrayBorder = Color(0xFFBDBDBD)        // Bordes sutiles

// Estados con colores Material Design 3
val SuccessGreen = Color(0xFF4CAF50)      // Verde éxito
val WarningAmber = Color(0xFFFFA726)      // Naranja advertencia (más cálido)
val ErrorRed = Color(0xFFEF5350)          // Rojo error (menos agresivo)
val InfoBlue = Color(0xFF42A5F5)          // Azul info

// Colores adicionales para componentes específicos
val BackgroundGradientStart = Color(0xFFF1F8F4)  // Degradado sutil verde
val BackgroundGradientEnd = Color(0xFFFFFFFF)

val CardElevation = Color(0x0D000000)     // Sombra sutil para elevación
val Divider = Color(0x1F000000)           // Divisores sutiles

// Colores semánticos para postura
val PostureGoodBackground = Color(0xFFE8F5E9)
val PostureGoodText = Color(0xFF2E7D32)
val PostureBadBackground = Color(0xFFFFEBEE)
val PostureBadText = Color(0xFFC62828)
val PostureWaitingBackground = Color(0xFFF5F5F5)
val PostureWaitingText = Color(0xFF616161)

// Overlay para estados (ej: botones presionados)
val RippleColor = Color(0x1F4CAF50)       // Efecto ripple verde sutil